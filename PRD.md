# PRD｜东方Project同人作品查询（my-website）

> 课程：21 天建站 · Day 4｜第 1 周
> 上游文档：research.md（Day 3 需求研究，2026-09-18）
> 本文档定义「做什么、做到什么程度算完成」；技术选型是 Day 5 的任务，本文不涉及。

---

## 1. 项目概述

**一句话定位**：一个面向中文用户的东方Project同人作品轻量查询站——不追求收录量，用精选的小数据集，把「查得爽」做出来。

- **要解决的问题**：东方同人作品查询需求分散在维基、商店、社区，没有一个面向中文普通爱好者（而非老手）的轻量查询入口（详见 research.md 第一节）。
- **差异化**：查询体验（模糊搜索、按「爱好者的问题」组织的聚合视角），不是数据量。
- **形态**：纯静态站（本地 JSON 数据，无后端），部署到 GitHub Pages。

## 2. 目标用户与核心场景

**用户**：中文东方Project爱好者中「只想随手查查」的普通爱好者——不是维护维基的编辑，也不是必须用规范词汇检索的老手。

**核心场景（每个对应一个功能模块）**：

| # | 场景（用户的实际问题） | 对应功能 |
|---|---|---|
| S1 | 「某社团出过哪些东西？」 | 社团查询（跨模块反查） |
| S2 | 「我喜欢这个角色，她登场于哪些同人作品？」 | 角色查询（跨模块反查） |
| S3 | 「翻翻某一年出了什么同人志」 | 年份分组浏览 |
| S4 | 「我记得模糊的词（角色名、半个社团名）」 | 模糊搜索 |
| S5 | 「这张专辑/这本本的详细信息是什么？去哪看更多？」 | 详情页 + 来源外链 |

## 3. 功能需求（MVP = Day 7 交付范围）

**通用规则**：所有列表页共享——搜索词与各筛选器之间为 AND；原作标签多选内部为 OR（如「红魔乡 OR 妖妖梦」）；无结果时显示明确空状态提示并给「清空筛选」按钮。

### F1 首页（= 分类入口页）

| 编号 | 功能 | 验收标准 |
|---|---|---|
| F1.1 | 三个分类卡片：同人音乐 / 同人志 / 同人游戏 | 点击卡片跳转对应模块查询页；每个模块页可直接通过 URL 访问（可分享、可刷新） |
| F1.2 | 两个查询入口：按角色查 / 按社团查 | 点击进入对应查询页，页面含输入框 |
| F1.3 | 热门角色快捷入口 | 展示「登场作品数」排名前 8 的角色（按数据统计）；点击任一角色 → 该角色的查询结果页 |

### F2 同人音乐模块（数据量：精选 20–30 张专辑）

| 编号 | 功能 | 验收标准 |
|---|---|---|
| F2.1 | 模糊搜索 | 输入关键词后，列表仅保留「专辑名 / 社团名 / 登场角色」任一字段含该词的专辑；输入「灵梦」能命中名称、社团、角色中含「灵梦」的条目 |
| F2.2 | 筛选：社团（单选下拉）、年份（单选）、原作标签（多选，OR）、原曲（输入匹配） | 任一筛选变更即时生效；四类筛选与搜索词可任意组合，结果符合通用规则；被筛掉的条目不出现；筛选后无结果时显示空状态提示与「清空筛选」按钮，点击后恢复全量列表 |
| F2.3 | 年份分组浏览 | 列表可切换「按年份分组」视图，组内按年份倒序，组标题为年份且各组带条目数 |
| F2.4 | 原曲筛选粒度 | 原曲按「专辑内任一曲目涉及该原曲」匹配（原曲数据挂在曲目级，见 D1） |

### F3 同人志（漫画）模块（数据量：精选 15–20 本）

| 编号 | 功能 | 验收标准 |
|---|---|---|
| F3.1 | 模糊搜索 | 同 F2.1，字段换为「作品名 / 社团名 / 登场角色」 |
| F3.2 | 筛选：社团、年份（单选）、原作标签（多选） | 同 F2.2（无原曲维度） |
| F3.3 | 年份分组浏览 | 同 F2.3 |

### F4 同人游戏模块（数据量：精选 10–15 个）

| 编号 | 功能 | 验收标准 |
|---|---|---|
| F4.1 | 模糊搜索 | 同 F2.1，字段换为「作品名 / 社团名 / 登场角色」 |
| F4.2 | 筛选：社团、年份（单选）、原作标签（多选） | 同 F2.2（无原曲维度） |
| F4.3 | 年份分组浏览 | 同 F2.3 |

### F5 角色查询（跨模块）

| 编号 | 功能 | 验收标准 |
|---|---|---|
| F5.1 | 输入或点选角色名 | 输入「博丽灵梦」→ 结果按三个模块分组展示该角色登场的全部作品，每组标题带数量；三个模块均无结果时显示空状态提示 |
| F5.2 | 与原作标签组合 | 角色查询结果内可用原作标签二次筛选（如「妖妖梦 × 八云紫」） |

### F6 社团查询（跨模块）

| 编号 | 功能 | 验收标准 |
|---|---|---|
| F6.1 | 输入或点选社团名 | 结果按三个模块分组展示该社团发布的全部作品，每组标题带数量；无结果时显示空状态提示 |
| F6.2 | 社团信息区 | 展示社团名 + 活跃年份范围（**由该社团作品年份自动推导**，不单独维护字段） |

### F7 详情页（三模块通用结构）

| 编号 | 功能 | 验收标准 |
|---|---|---|
| F7.1 | 信息展示 | 每条作品展示：名称、社团、分类专属字段（见 D1–D3）、登场角色、原作标签、发售年份、简介 |
| F7.2 | 来源外链 | 每条详情含「资料来源」链接（THBWiki 等来源页），点击新标签页打开 |
| F7.3 | URL 直达 | 每条作品有独立 URL，可分享、可刷新 |

## 4. 数据需求

**D1 同人音乐字段**：专辑名、社团、发售年份、登场角色[]、原作标签[]、原曲[]（曲目级，聚合到专辑）、作曲者[]（Day 21 前补充利用，MVP 先入库不筛选）、简介、来源链接。
**D2 同人志字段**：作品名、社团、发售年份、登场角色[]、原作标签[]、简介、来源链接。
**D3 同人游戏字段**：作品名、社团、发售年份、登场角色[]、原作标签[]、简介、来源链接。

- 数据形态：本地 JSON，手工整理自公开资料（THBWiki 等），**只收录公开元数据，不含正文内容与图片资源**（版权红线）。
- 原作标签 = 可多选的东方原作游戏（红魔乡、妖妖梦、永夜抄、风神录……）。
- **Day 4 抽样验证结论**（research.md 第七节遗留项）：抽验 5 张专辑词条，登场角色、逐曲目原曲、社团、首发日期四个字段全部可得 → 原曲筛选保留；角色字段直接存在于专辑页，无需按「主打角色原曲归属」推导（比 Day 3 假设更简单）。

## 5. 非功能需求

| 编号 | 需求 | 验收标准 |
|---|---|---|
| N1 | 性能：筛选/搜索为本地计算 | 搜索与筛选响应 < 100ms |
| N2 | 加载：静态资源轻量 | GitHub Pages 上首屏可交互 < 2s（常规网络） |
| N3 | 兼容 | 最新版 Chrome / Edge / Firefox 可用；375px 宽（移动端）布局不错乱 |
| N4 | 无后端 | 全部 MVP 功能无需任何服务端，纯静态可部署 |
| N5 | 暗色模式 | 非 MVP，Day 21 前完成（延续此前低成本决定） |

## 6. 砍单记录（今日核心：砍了什么、为什么砍）

**砍单判断标准**（三条，满足任一条即砍）：① 依赖不存在的东西（无后端/无数据库）；② 与差异化（查询体验）无关；③ 法律或技术风险高。

### 6.1 沿用 Day 3 调研结论的砍单

| 砍掉的功能 | 归因 |
|---|---|
| 用户账号、注册登录 | ①：静态站无后端，课程 Day 23 才接触数据库 |
| 收藏 / 评分 / 评论 | ①：需后端与用户体系 → 二期需求池 |
| UGC 编辑 / 投稿 | ②+③：数据质量控制成本高，THBWiki 已承担该角色 |
| 爬虫自动同步 THBWiki / Melonbooks | ③：技术与版权风险双高，本期数据手工整理 |
| 追求收录量（「全库」） | ②：差异化在查询体验不在数据量 |
| 在线试听 / 阅读 | ③：版权红线，只做元数据 + 外链 |
| 购买 / 代购跳转合作 | ②：商业功能远超课程范围 |
| 日文界面 / 多语言 | ②：作品名本身中日混杂，多语言收益低成本高 |

### 6.2 PRD 阶段（Day 4）新砍的

| 砍掉的功能 | 归因 | 说明 |
|---|---|---|
| **年份区间筛选**（如 2010–2015） | ② | 区间筛选的 UI 与逻辑成本约为单选的 3 倍，而「翻某一年出了什么」的年份分组浏览已覆盖年代式浏览需求，收益重复。保留：年份单选筛选 + 年份分组浏览 |
| **社团「主要活动领域」字段** | ② | 需额外手工维护一份社团元数据；MVP 的社团页改用从作品年份**自动推导**的活跃范围，数据能推导就不维护 |
| **原作标签之外的「原曲多选标签墙」** | ② | 原曲数量达数百首，做标签墙 UI 不可用；原曲维度改为**输入框匹配**（见 F2.2/F2.4） |

> 砍掉 ≠ 永远不做：以上全部进二期需求池，MVP 上线后按用户反馈捞回。

## 7. 里程碑

| 节点 | 交付 | 验收 |
|---|---|---|
| Day 7 | MVP：F1–F7 全部功能 + 三模块精选数据 | 第 3 节全部验收标准逐条通过 |
| Day 21 | 收尾：数据扩到百条级、暗色模式（N5）、音乐模块作曲者筛选、同人志题材标签 | 届时另列验收标准 |

**降级预案**（对应课程的「卡住降级」）：若 Day 7 时间不足，先交付 首页 + 同人音乐模块 + 详情页，同人志/同人游戏模块与角色/社团跨模块查询列入 Day 8–10 补齐——模块化结构（Day 3 设计决定）保证了可以分批做。

## 8. 附：用户故事（Day 4 余力加练）

**F2.1 模糊搜索**的用户故事：

> **作为**一个只想随手查查的东方爱好者，
> **我想要**在搜索框里输入记得模糊的词（比如角色名「灵梦」、半个社团名），
> **以便**不用知道 THBWiki 式的规范词汇，也能找到相关的同人作品。
>
> - **Given** 同人音乐模块已加载 20–30 张专辑数据
> - **When** 我在搜索框输入「灵梦」
> - **Then** 列表只剩名称、社团或登场角色中含「灵梦」的专辑，响应 < 100ms，我能从结果点进任一专辑的详情页

## 9. 附：Day 4 AI 自检记录

自检时间：2026-09-19；自检人：AI 助手（WorkBuddy）；依据：第 10 节 5 条标准 + 与 research.md 的一致性比对。

| 检查项 | 结果 |
|---|---|
| 1. 每个功能有可判定验收标准 | ✅（自检发现 F1.3 表述有歧义，已改写） |
| 2. 与 research.md MVP 范围一致 | ✅（差异 3 处均在 6.2 节注明理由：年份区间、社团领域字段、原曲改输入匹配） |
| 3. 砍单均有归因 | ✅（6.1 八条 + 6.2 三条，各归因 ①/②/③） |
| 4. 数据量与课程窗口匹配 | ✅（三模块合计 45–65 条，手工整理可行） |
| 5. 全部功能无后端可实现 | ✅（URL 直达/直达分享可用前端路由实现，技术细节属 Day 5） |

**自检中修改的问题**：F1.3 验收标准歧义（必须级，已改写）；F2.2 补空状态与清空筛选的验收锚点（建议级，已补）；S2 场景语序（建议级，已顺）。**修改后复检：无「必须修改」级遗留。**

## 10. 整体验收标准（本文档自身的完成标准）

1. 第 3 节每个 MVP 功能均有可判定的验收标准（给定→操作→结果可测）；
2. MVP 范围与 research.md 第四节一致，差异均在第 6.2 节注明理由；
3. 每条砍单均有归因（三条标准之一），无「默默不做」；
4. 数据量（20–30 / 15–20 / 10–15）与 21 天课程窗口匹配，手工整理可行；
5. 全部 MVP 功能满足 N4（无后端可实现）。

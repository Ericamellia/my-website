# TECH_DESIGN.md｜技术设计与数据流

> 课程：21 天建站 · Day 5｜第 1 周
> 上游文档：PRD.md（Day 4）
> 本文档定义「怎么实现、用什么技术、为什么」；具体代码是 Day 7 的任务。
> 今日不做：写代码；回头大改 PRD。

---

## 0. 三句话先讲清前后端与数据库的分工

| 角色 | 在哪跑 | 干什么 |
|---|---|---|
| **前端**（HTML/CSS/JS） | 用户浏览器里 | 你看到的页面、点击交互、显示列表/详情 |
| **后端**（Node / Python / Java 等） | 服务器上 | 处理业务逻辑、读数据库、决定返回什么数据 |
| **数据库**（MySQL / PostgreSQL / MongoDB 等） | 服务器上 | 专门存数据的地方，支持增删改查 |

**静态站 = 只需要前端 + 数据文件**，浏览器直接打开，无需后端和数据库。GitHub Pages 就是最常见的静态站托管。

**本项目为什么不需要后端和数据库**（对应 PRD）：
- PRD 第 5 节 N4 已硬约束：全部 MVP 功能无需任何服务端
- 数据量小：精选 45–65 条（手工整理），前端 JS 计算就够快
- 没用户账号、没 UGC、没爬虫——没有「必须后端才能做的事」（PRD 第 6 节砍单记录）

→ 因此技术路线 = **纯静态 + 本地 JSON**，不引入后端和数据库。

---

## 1. 技术路线（一句话 + 为什么）

**一句话**：纯前端（原生 HTML/CSS/JS） + 本地 JSON 数据 + URL hash 路由 + GitHub Pages 部署，不引入构建工具、不引入框架、不引入后端与数据库。

### 为什么暂时选它（卡住降级原则：示范路线，理由先写清楚）

| # | 理由 | 对应约束 / 现实 |
|---|---|---|
| ① | PRD N4 已硬约束「全部 MVP 功能无需任何服务端」 | PRD 第 5 节 |
| ② | 数据量 45–65 条，本地 JS 计算 < 100ms 即可 | PRD N1（性能） + 课程 Day 7 时间紧 |
| ③ | 部署成本最低：git push 即发布、无服务器费用、无运维 | 课程范围 / 个人项目 |
| ④ | 不引入框架与构建工具 = 学习成本最低、出问题最快定位 | 用户节奏「看懂概念 + 指挥 AI 干活」 |
| ⑤ | 后续可平滑演进：需要时再拆后端 / 换框架 / 接数据库 | 静态站的天然可升级路径 |

> **降级说明**：本路线为示范路线，未做多方案比较（框架层在第 6 节风险与降级中有简要回看）。若 Day 7 实测遇到瓶颈，按对应降级方案处理。

---

## 2. 技术栈分层

| 层 | 选型 | 理由 |
|---|---|---|
| **页面结构** | HTML5 | 标准、无依赖 |
| **样式** | 原生 CSS + CSS 变量 | 暗色模式靠 `prefers-color-scheme` + CSS 变量切换（Day 21 前） |
| **脚本** | 原生 JavaScript（ES2020+） | 数据量小、无复杂状态管理；引入框架的成本 > 收益 |
| **数据** | 本地 JSON 文件（手工整理） | git 提交即版本管理；浏览器 fetch 即加载 |
| **路由** | URL hash 路由（`#/music` 这种） | 静态托管无需 server 配置；可分享、可刷新 |
| **构建** | 无（直接 `index.html` + `.js` 文件） | MVP 阶段不引入 Vite / webpack，复杂度最低 |
| **部署** | GitHub Pages | PRD 已确定；与 git 仓库一体，commit 即部署 |
| **搜索 / 筛选 / 排序** | 纯前端 JS（`Array.filter` / `Array.sort`） | 45–65 条数据，前端计算 < 1ms |

---

## 3. 数据流图（数据从哪来、到哪去）

```
┌────────────────────────────────────┐
│  离线（数据生产，人工）             │
├────────────────────────────────────┤
│                                    │
│  THBWiki / Melonbooks 等公开页     │  ← 公开资料源（手工查阅）
│              ↓                     │
│  人工录入 + 标注热度（PRD §4）      │  ← 数据生产步骤（一次性、离线）
│              ↓                     │
│  data/music.json   (20–30 条)      │
│  data/doujin.json  (15–20 条)      │  ← 三份 JSON，git 仓库内
│  data/game.json    (10–15 条)      │
│              ↓                     │
│  git add / commit                  │
│              ↓                     │
│  git push → GitHub main 分支       │
│                                    │
└──────────────┬─────────────────────┘
               │
               │  运行时（用户访问网站）
               ↓
┌────────────────────────────────────┐
│  在线（数据消费，浏览器内）         │
├────────────────────────────────────┤
│                                    │
│  浏览器请求首页 URL                │
│              ↓                     │
│  GitHub Pages 返回                  │  ← index.html / style.css / app.js
│  静态文件                          │
│              ↓                     │
│  浏览器执行 app.js                 │
│              ↓                     │
│  fetch('data/*.json')              │  ← 从 GitHub Pages 拉 JSON
│              ↓                     │
│  解析为内存对象（全局缓存）         │
│              ↓                     │
│  搜索 / 筛选 / 排序 / 分组         │  ← 纯前端 JS 计算（< 100ms）
│              ↓                     │
│  DOM 渲染                          │  ← 列表 / 详情 / 分组视图
│                                    │
└────────────────────────────────────┘
```

### 一句话数据通路

> 数据从 THBWiki 等公开页出发，被人工录入为本地 JSON 文件，提交到 git 仓库并通过 GitHub Pages 静态托管；用户浏览器打开页面时，JS fetch 加载 JSON 到内存，前端计算后把结果渲染到 DOM。

### URL 路由 → 数据视图（怎么从 URL 走到具体页面）

```
URL（如 #/music、#/circle/某社团）
        ↓ 监听 hashchange
当前模块标识 + 当前 URL 参数
        ↓ 加载对应 JSON + 解析筛选条件
搜索 / 筛选 / 排序 / 分组（纯前端 JS）
        ↓
结果集
        ↓ renderList / renderDetail / renderGrouped
DOM（用户看到的页面）
```

---

## 4. 关键模块的技术实现思路（Day 7 写代码时遵循）

### 4.1 三模块列表页（F2 / F3 / F4）

```
读取对应 JSON → 根据 URL 参数（搜索词、筛选器、排序方式）
   → filter() 过滤 → sort() 排序 → 渲染列表
```

- **模糊搜索（F2.1）**：`String.includes()` 在 `专辑名 / 社团名 / 登场角色` 三个字段上做包含匹配；多字段之间为 AND
- **筛选器（F2.2）**：每个筛选项对应一个 `Array.filter` 步骤，链式组合；原作标签多选内部为 OR
- **排序切换（F2.5）**：默认按 `发售年份倒序`；切换后按 `热度降序`，同热度按年份倒序
- **年份分组浏览（F2.3）**：在排序结果上 `groupBy('year')`

### 4.2 角色查询 / 社团查询（F5 / F6，跨模块）

```
读取全部三份 JSON → 在每条记录的「登场角色[]」或「社团」字段上做包含匹配
   → 按模块分组（music / doujin / game 三组）→ 渲染分组视图
```

- 与模块列表页共用同一份数据，**零新数据、零新页面**
- F6.3 社团名点击跳转：渲染时把社团名包成 `<a href="#/circle/{name}">` 即可

### 4.3 详情页（F7）

```
URL（#/music/{id}） → 根据模块与 id 在 JSON 中查找 → 渲染详情
```

- URL 直达 = 浏览器刷新或分享链接都能进入同一详情页
- 「资料来源」链接 = `<a target="_blank" href="...">`

### 4.4 空状态与清空筛选（PRD 通用规则）

```
结果数 === 0  → 显示「无匹配」+「清空筛选」按钮
点击清空  → URL 参数重置 + 重新渲染
```

---

## 5. 仓库文件结构（Day 7｜v2：升级到 `my-app/` 子目录）

> **v2 变更**（2026-09-21 Day 7）：依据 AGENTS.md 末尾个人规则「代码放 my-app 文件夹里」→ 把 v1 全部代码文件（index.html / CSS / JS / JSON）从仓库根目录迁到 `my-app/` 子目录。
> v1 旧结构在 git 历史 Day 5 commit `8c3a33d` 可查；v2 起一切新增走新位置。

```
my-website/
├── my-app/                  ← Day 7 新增：代码工作区（遵守 AGENTS.md 个人规则）
│   ├── index.html           首页（分类入口，含搜索框 + 三模块卡片 + 角色/社团入口）
│   ├── style.css            样式（含暗色模式变量，Day 21 前补）
│   ├── app.js               主脚本（URL hash 路由 + 数据加载 + 渲染）
│   └── data/
│       ├── music.json       同人音乐 20–30 条（Day 7 MVP 用 5–10 条 mock）
│       ├── doujin.json      同人漫画 15–20 本（PRD F3；Day 7 MVP 用 5–8 条 mock）
│       └── game.json        同人游戏 10–15 条（Day 7 MVP 用 3–5 条 mock）
├── PRD.md                   （Day 4 课程版）
├── research.md              （Day 3）
├── TECH_DESIGN.md           ← 本文件
├── AGENTS.md                （Day 6）
├── index.html               （Day 2 占位页 —— 保留便于看旧里程碑，Day 7 以后更新看 my-app/index.html）
└── .gitignore               （Day 2）
```

- **v1 → v2 改了什么**：v1 把 index.html / CSS / JS 放根目录，v2 全部进 `my-app/`
- **为什么改**：跟 AGENTS.md 个人规则一致；将来加 npm / Vite / 构建步骤时，`my-app/` 作根目录比仓库根目录清爽
- **数据规模**：PRD 满编（音乐 20-30 / 漫画 15-20 / 游戏 10-15）要几十条人工数据录入（详见 research.md 第二节），Day 7 MVP 先用 5-10 条 mock 跑通骨架；二期按 PRD 数据字段扩展
- **GitHub Pages 部署**：默认从仓库根目录服务，要从 `my-app/` 部署需配置 Pages source = `my-app/ on branch main`（Settings → Pages）；Day 21 前不动这块，本地开发走 `python -m http.server 8000` 后访问 `http://localhost:8000/my-app/` 即可

---

## 6. 风险与降级

| 风险 | 触发条件 | 降级方案 |
|---|---|---|
| 数据量涨到 500+ 条，本地计算超 100ms | Day 21 后扩数据 | 引入轻量倒排索引（按角色 / 社团 / 原作标签预建） |
| 多 JSON 加载顺序问题 | 网络抖动 | 单次并发 `fetch` + `Promise.all` |
| GitHub Pages 缓存导致更新看不到 | 发布新版 | 浏览器 hard refresh（Ctrl+Shift+R）；或加 `?v=<commit-sha>` 强制刷新 |
| URL hash 路由被搜索引擎收录差 | 二期想 SEO | 切换 History API（需 GitHub Pages 自定义 404 → index.html 转发；二期再说） |
| 想接真实数据库 / 多用户 | 二期想加账号 / UGC | 引入 Firebase / Supabase 等 BaaS；本静态站先稳住 |
| 想换前端框架 | 代码量上来后维护成本升高 | 评估 Vue / Svelte 等轻量框架；本日暂不引入 |

> **未做多方案比较的诚实说明**：今天只选了「原生 JS + hash 路由 + GitHub Pages」这一条示范路线。如果想看框架 / 构建工具 / 部署平台三轴的横向对比，可以放到二期加练。

---

## 7. 整体验收标准（本文档自身的完成标准）

1. §1 用一句话说清技术路线 + 给出理由（≥ 3 条）
2. §2 技术栈分层表覆盖 前端 / 数据 / 路由 / 部署 / 计算 五个层面
3. §3 数据流图能从「公开资料 → JSON → 浏览器 → DOM」一路画完
4. §4 关键模块的实现思路覆盖 F2 / F3 / F4 / F5 / F6 / F7 六个功能
5. §5 文件结构与现有仓库兼容（PRD.md / research.md / index.html / .gitignore 不动）
6. §6 至少 3 条风险与对应降级方案

---

> 配套截图交付要求 = 打开的 TECH_DESIGN.md，图中能看见数据流图 + 技术路线那一行。
